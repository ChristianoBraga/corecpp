window.docContents[131].resolve({"/Abstraction/Call/#Core-C______--Abstraction--Call":{"contents":"Let  be in the program. The environment of the function holds only the parameters, because there are no global variables.Each argument is checked at the type of its parameter by the judgment  of , so that a lambda may be an argument. A variable f of function type in  hides the function named f, and the call is then .The return frees the copies of the arguments and the locals the body declared,  read as the owned bindings the body added, so after a call the store holds only what existed before it and the objects created with new. When the body ends with , the result is  if  and error otherwise. A missing return in a non void function is an evaluation error, not a type error. The rule above is the case in which every parameter is by value, and  gives the case of a parameter by reference.\n\n","context":"Core C++\u0009Abstraction","header":"5.1. Call","id":"/Abstraction/Call/#Core-C______--Abstraction--Call"}});