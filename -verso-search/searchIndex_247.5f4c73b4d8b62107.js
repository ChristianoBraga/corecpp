window.docContents[247].resolve({"/UD-VI___-type-systems/Overloading/#Core-C______--UD-VI___-type-systems--Overloading":{"contents":"A name denotes an overload set, the functions of the program with that name, or the methods of that name along the chain of a class, one per signature. A method of a derived class replaces the one of a base with the same signature, and a different signature is another overload, so the subset has no name hiding. Two declarations of one name live together only when they differ.The restriction on std::function is the check 5 of the design. It keeps a lambda argument from deciding a call, which would ask for the type of the lambda before the candidate that gives it is known.\n\nA call collects the candidates of its arity that accept the arguments, each argument by value acceptable at the type of its parameter and each argument of a reference parameter denoting a location of that type. The choice is the exact candidate when several accept.With  empty the error is the one of the single candidate of that arity, when there is one, and no overload otherwise. With two or more in  and no exact one the call is ambiguous. Core C++ does not rank conversion sequences as C++ does, so the rule fits in one line on the board and the error is predictable.\n\nOverloading adds no evaluation rule. The type checker writes the signature of the chosen candidate into the tree, the field sig of a call, and the evaluator looks a function or a method up by name and signature instead of by name alone. A program that was not annotated, which the checker rejects first, runs with the first candidate of the name.\n\n","context":"Core C++\u0009UD VI, type systems","header":"7.1. Overloading","id":"/UD-VI___-type-systems/Overloading/#Core-C______--UD-VI___-type-systems--Overloading"}});