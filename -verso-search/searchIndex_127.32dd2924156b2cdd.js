window.docContents[127].resolve({"/Judgments-and-domains/Domains/#Core-C______--Judgments-and-domains--Domains":{"contents":"A location  is a natural number. Locations are never reused.\n\nThe values are , , , the pointer , the value  of nullptr, the object , a record of one location per field with its class tag, and the vector , one location per element. The value  is the result of a call to a function without return value, and the initial content of a field of function type before the constructor assigns it. UD IV adds the closure, . Since UD V the tag of an object is the class it was created with, its fields are those of the whole chain of classes, the root base first, and the tag decides the dispatch of virtual methods and the destructors that delete runs. Objects and vectors live in the store at their own location and are reached only through pointers. The default value of a type, which new gives to every field and element, is ,  or .\n\nThe type int has 32 bits in two's complement. The partial operation  returns the integer when it lies in the range and error otherwise.\n\nThe environment  is a finite map from identifiers to locations. The notation  extends , and the most recent binding prevails. Each binding records whether the declaration that made it allocated the location, as τ x = e does, or aliased an existing one, as the reference τ& y = e does. Block exit frees only the owned locations. Inside a member body, this is an alias binding to the location of the receiver, made by the call and never freed by the return.\n\nThe store  is a finite map from locations to values. The operation  returns a fresh location  and the store . The operation  removes the locations of  from the domain. Reading or writing outside the domain is error.\n\nThe value of a lambda is the closure , with the parameters, the result type, the body and the captured copies, one value per free variable of the body. The types std::function<τ(τ₁, …, τₖ)> have closures as values. A variable or a parameter holds a closure, a field or a vector element does not, because new would have to give it a default value and there is no empty function in this subset.\n\nThe control result  of a command is  or . A  interrupts sequence, block and loop up to the call that consumes it.\n\nThe result error is not a value of the language. It replaces the result of any dynamic judgment and propagates to the whole program. Its causes are division by zero, int overflow, the dereference of nullptr, an index outside a vector, a negative vector size, a location outside , an undeclared variable or function, wrong arity, a missing return in a non void function, a second delete of the same object, and a delete through a pointer to a base class without a virtual destructor.\n\nThe typing context  is a finite map from identifiers to types. The types are , , , a class , a pointer , a vector  and the internal type  of nullptr. Class and vector types are object types, they have no values, and no variable, parameter, result or field has one. An expression of object type occurs only as the operand of ., [] or *. Since UD IV each binding carries a mark, read only or not. The mark is set on every binding of the enclosing scope when the body of a lambda is checked, so that the copies of [=] are read and never written.\n\nA program is a list of declarations, classes and functions. The functions form a finite map from names to declarations, fixed during the whole evaluation and implicit in the judgments.\n\nThe classes of the program form the class table, a finite map from class names to declarations. A declaration has an optional base, fields and methods with their visibility, at most one constructor and at most one destructor. The table gives, for a class, its chain up to the root base, every field of the chain with the class that declares it, the nearest method of a given name, the subclass relation and whether some class of the chain has a virtual destructor. The type checker consults it for types, visibility and dispatch, and new and delete for the fields to allocate and to free. Names are qualified by their namespace, N::C.\n\n","context":"Core C++\u0009Judgments and domains","header":"2.1. Domains","id":"/Judgments-and-domains/Domains/#Core-C______--Judgments-and-domains--Domains"}});