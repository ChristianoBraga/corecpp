import Verso
import VersoManual
import VersoBlueprint
import CoreCpp.Token
import CoreCpp.Lexer
import CoreCpp.Syntax
import CoreCpp.Parser
import CoreCpp.LL1
import CoreCpp.Grammar

open Verso.Genre
open Verso.Genre.Manual
open Informal

set_option verso.blueprint.foldCodeBlocks true

#doc (Manual) "Lexer and grammar" =>

Core C++ has an LL(1) grammar over tokens. Three lexical and syntactic conventions remove the ambiguities of C++. Type identifiers start with an uppercase letter and variable identifiers with a lowercase one, which decides in the lexer whether `<` opens a template argument or compares. Template instantiation occurs only in type position. Methods are defined inside the class. The lexer is a hand written finite automaton with the longest match rule, and the parser is recursive descent with one function per nonterminal.

:::group "ud1"
Syntax, tokens and the parser.
:::

# Lexer

:::definition "lex_tokens" (parent := "ud1") (lean := "CoreCpp.Token, CoreCpp.keywords, CoreCpp.symbols3, CoreCpp.symbols2, CoreCpp.symbols1")
Tokens fall in five classes. Reserved words, among them `int`, `bool`, `void`, `if`, `else`, `while`, `for`, `return`, `true`, `false`, `auto`, `class`, `new`, `delete`, `nullptr`, `this`, `virtual`, `override`, `namespace`, `template`, `typename` and `operator`. Type identifiers, `TypeId`, with an uppercase initial. Variable identifiers, `VarId`, with a lowercase initial, naming variables, fields, functions and methods. Decimal integer literals, `IntLit`. Operators and punctuation, with `[=]` as a single token by the longest match rule. There is no token `>>`, so `Stack<Stack<int>>` closes with two tokens `>`.

$$`\begin{array}{lcl} \textit{Token} & ::= & \textit{Keyword}\ \mid\ \textit{TypeId}\ \mid\ \textit{VarId}\ \mid\ \textit{IntLit}\ \mid\ \textit{Symbol} \\ \textit{Keyword} & ::= & \texttt{int}\ \mid\ \texttt{bool}\ \mid\ \texttt{void}\ \mid\ \texttt{class}\ \mid\ \texttt{public}\ \mid\ \texttt{private}\ \mid\ \texttt{new}\ \mid\ \texttt{delete} \\  & \mid & \texttt{nullptr}\ \mid\ \texttt{this}\ \mid\ \texttt{virtual}\ \mid\ \texttt{override}\ \mid\ \texttt{namespace}\ \mid\ \texttt{template} \\  & \mid & \texttt{typename}\ \mid\ \texttt{auto}\ \mid\ \texttt{if}\ \mid\ \texttt{else}\ \mid\ \texttt{while}\ \mid\ \texttt{for}\ \mid\ \texttt{return} \\  & \mid & \texttt{true}\ \mid\ \texttt{false}\ \mid\ \texttt{operator}\ \mid\ \texttt{std::function}\ \mid\ \texttt{std::vector} \\ \textit{TypeId} & ::= & \textit{Upper}\ \textit{IdentChar}^{*} \\ \textit{VarId} & ::= & \big(\ \textit{Lower}\ \mid\ \texttt{\_}\ \big)\ \textit{IdentChar}^{*} \\ \textit{IntLit} & ::= & \textit{Digit}\ \textit{Digit}^{*} \\ \textit{Symbol} & ::= & \texttt{[=]} \\  & \mid & \texttt{==}\ \mid\ \texttt{!=}\ \mid\ \texttt{<=}\ \mid\ \texttt{>=}\ \mid\ \texttt{\&\&}\ \mid\ \texttt{||}\ \mid\ \texttt{->}\ \mid\ \texttt{::} \\  & \mid & \texttt{+}\ \mid\ \texttt{-}\ \mid\ \texttt{*}\ \mid\ \texttt{/}\ \mid\ \texttt{\%}\ \mid\ \texttt{<}\ \mid\ \texttt{>}\ \mid\ \texttt{!}\ \mid\ \texttt{?}\ \mid\ \texttt{:}\ \mid\ \texttt{=} \\  & \mid & \texttt{(}\ \mid\ \texttt{)}\ \mid\ \texttt{\{}\ \mid\ \texttt{\}}\ \mid\ \texttt{[}\ \mid\ \texttt{]}\ \mid\ \texttt{,}\ \mid\ \texttt{;}\ \mid\ \texttt{.}\ \mid\ \texttt{\&}\ \mid\ \texttt{\textasciitilde} \\ \textit{IdentChar} & ::= & \textit{Upper}\ \mid\ \textit{Lower}\ \mid\ \textit{Digit}\ \mid\ \texttt{\_} \\ \textit{Upper} & ::= & \texttt{A}\ \mid\ \ldots\ \mid\ \texttt{Z} \\ \textit{Lower} & ::= & \texttt{a}\ \mid\ \ldots\ \mid\ \texttt{z} \\ \textit{Digit} & ::= & \texttt{0}\ \mid\ \ldots\ \mid\ \texttt{9} \\ \textit{Skip} & ::= & \textit{WhiteSpace}\ \mid\ \texttt{//}\ \textit{NotNewline}^{*}\ \textit{Newline} \end{array}`

The input is the longest sequence of `Token` and `Skip` that covers it, `Skip` is discarded, and each token is the longest match at its position. The symbols of three characters are tried before those of two and of one.
:::

:::definition "lex_conventions" (parent := "ud1") (lean := "CoreCpp.Lexer.identifier") (uses := "lex_tokens")
An identifier is classified by its initial after the reserved words are excluded. The names `std::function` and `std::vector` are one token each, recognised before the identifier rule because `std` starts with a lowercase letter.
:::

:::definition "lex_automaton" (parent := "ud1") (lean := "CoreCpp.lex, CoreCpp.Lexer.run, CoreCpp.Lexer.takeWhile, CoreCpp.Lexer.matchSymbol, CoreCpp.Lexer.skipLine") (uses := "lex_tokens, lex_conventions")
The lexer maps a string to an array of tokens ended by `eof`. It discards white space and line comments, reads the longest run of digits as an integer literal, the longest run of identifier characters as an identifier, and tries the symbols of three, two and one characters in that order. Any other character is a lexical error.
:::

# Grammar

:::definition "gram_full" (parent := "ud1")
The grammar of Core C++, in EBNF. The superscript $`X^{*}` repeats $`X` zero or more times, the superscript $`X^{?}` makes $`X` optional, large parentheses group, the bar separates alternatives and terminals are in typewriter font. The grammar has no left recursive production, and it is left factored.

$$`\begin{array}{lcl} \textit{Program} & ::= & \textit{Declaration}^{*} \\ \textit{Declaration} & ::= & \texttt{namespace}\ \textit{TypeId}\ \texttt{\{}\ \textit{Declaration}^{*}\ \texttt{\}} \\  & \mid & \texttt{template}\ \texttt{<}\ \texttt{typename}\ \textit{TypeId}\ \texttt{>}\ \textit{Class} \\  & \mid & \textit{Class} \\  & \mid & \textit{Function} \\ \textit{Class} & ::= & \texttt{class}\ \textit{TypeId}\ \big(\ \texttt{:}\ \texttt{public}\ \textit{ClassType}\ \big)^{?}\ \texttt{\{}\ \textit{Section}^{*}\ \texttt{\}}\ \texttt{;} \\ \textit{Section} & ::= & \big(\ \texttt{public}\ \mid\ \texttt{private}\ \big)\ \texttt{:}\ \textit{Member}^{*} \\ \textit{Member} & ::= & \texttt{virtual}\ \big(\ \textit{Type}\ \textit{VarId}\ \textit{Params}\ \textit{Block}\ \mid\ \texttt{\textasciitilde}\ \textit{TypeId}\ \texttt{(}\ \texttt{)}\ \textit{Block}\ \big) \\  & \mid & \texttt{\textasciitilde}\ \textit{TypeId}\ \texttt{(}\ \texttt{)}\ \textit{Block} \\  & \mid & \big(\ \textit{BasicType} \\  & & \qquad \mid\ \texttt{std::function}\ \texttt{<}\ \textit{Type}\ \texttt{(}\ \big(\ \textit{Type}\ \big(\ \texttt{,}\ \textit{Type}\ \big)^{*}\ \big)^{?}\ \texttt{)}\ \texttt{>} \\  & & \qquad \mid\ \texttt{std::vector}\ \texttt{<}\ \textit{Type}\ \texttt{>}\ \texttt{*}^{?}\ \big)\ \texttt{\&}^{?}\ \textit{MemberRest} \\  & \mid & \textit{TypeId}\ \big(\ \textit{Params}\ \textit{Block}\ \mid\ \textit{ClassTypeRest}\ \texttt{*}^{?}\ \texttt{\&}^{?}\ \textit{MemberRest}\ \big) \\ \textit{MemberRest} & ::= & \textit{VarId}\ \big(\ \texttt{;}\ \mid\ \textit{Params}\ \texttt{override}^{?}\ \textit{Block}\ \big) \\  & \mid & \texttt{operator}\ \textit{Op}\ \textit{Params}\ \textit{Block} \\ \textit{Op} & ::= & \texttt{+}\ \mid\ \texttt{-}\ \mid\ \texttt{*}\ \mid\ \texttt{/}\ \mid\ \texttt{\%}\ \mid\ \texttt{==}\ \mid\ \texttt{!=}\ \mid\ \texttt{<}\ \mid\ \texttt{<=}\ \mid\ \texttt{>}\ \mid\ \texttt{>=}\ \mid\ \texttt{[}\ \texttt{]} \\ \textit{Function} & ::= & \textit{Type}\ \textit{VarId}\ \textit{Params}\ \textit{Block} \\ \textit{Params} & ::= & \texttt{(}\ \big(\ \textit{Param}\ \big(\ \texttt{,}\ \textit{Param}\ \big)^{*}\ \big)^{?}\ \texttt{)} \\ \textit{Param} & ::= & \textit{Type}\ \textit{VarId} \\ \textit{Type} & ::= & \big(\ \textit{BasicType}\ \mid\ \big(\ \textit{ClassType}\ \mid\ \texttt{std::vector}\ \texttt{<}\ \textit{Type}\ \texttt{>}\ \big)\ \texttt{*}^{?} \\  & & \qquad \mid\ \texttt{std::function}\ \texttt{<}\ \textit{Type}\ \texttt{(}\ \big(\ \textit{Type}\ \big(\ \texttt{,}\ \textit{Type}\ \big)^{*}\ \big)^{?}\ \texttt{)}\ \texttt{>}\ \big)\ \texttt{\&}^{?} \\ \textit{BasicType} & ::= & \texttt{int}\ \mid\ \texttt{bool}\ \mid\ \texttt{void} \\ \textit{ClassType} & ::= & \textit{TypeId}\ \textit{ClassTypeRest} \\ \textit{ClassTypeRest} & ::= & \big(\ \texttt{::}\ \textit{TypeId}\ \big)^{*}\ \big(\ \texttt{<}\ \textit{Type}\ \big(\ \texttt{,}\ \textit{Type}\ \big)^{*}\ \texttt{>}\ \big)^{?} \\ \textit{Block} & ::= & \texttt{\{}\ \textit{Statement}^{*}\ \texttt{\}} \\ \textit{Statement} & ::= & \textit{Block} \\  & \mid & \texttt{if}\ \texttt{(}\ \textit{Expr}\ \texttt{)}\ \textit{Block}\ \big(\ \texttt{else}\ \textit{Block}\ \big)^{?} \\  & \mid & \texttt{while}\ \texttt{(}\ \textit{Expr}\ \texttt{)}\ \textit{Block} \\  & \mid & \texttt{for}\ \texttt{(}\ \textit{ForInit}\ \texttt{;}\ \textit{Expr}\ \texttt{;}\ \textit{ExprStatement}\ \texttt{)}\ \textit{Block} \\  & \mid & \texttt{return}\ \textit{ArgExpr}^{?}\ \texttt{;} \\  & \mid & \texttt{delete}\ \textit{Expr}\ \texttt{;} \\  & \mid & \textit{LocalDecl}\ \texttt{;} \\  & \mid & \textit{ExprStatement}\ \texttt{;} \\ \textit{LocalDecl} & ::= & \texttt{auto}\ \textit{VarId}\ \texttt{=}\ \textit{Expr} \\  & \mid & \textit{Type}\ \textit{VarId}\ \texttt{=}\ \textit{ArgExpr} \\ \textit{ForInit} & ::= & \textit{LocalDecl}\ \mid\ \textit{ExprStatement} \\ \textit{ExprStatement} & ::= & \textit{Expr}\ \big(\ \texttt{=}\ \textit{Expr}\ \big)^{?} \\ \textit{Expr} & ::= & \textit{OrExpr}\ \big(\ \texttt{?}\ \textit{Expr}\ \texttt{:}\ \textit{Expr}\ \big)^{?} \\ \textit{OrExpr} & ::= & \textit{AndExpr}\ \big(\ \texttt{||}\ \textit{AndExpr}\ \big)^{*} \\ \textit{AndExpr} & ::= & \textit{EqExpr}\ \big(\ \texttt{\&\&}\ \textit{EqExpr}\ \big)^{*} \\ \textit{EqExpr} & ::= & \textit{RelExpr}\ \big(\ \big(\ \texttt{==}\ \mid\ \texttt{!=}\ \big)\ \textit{RelExpr}\ \big)^{*} \\ \textit{RelExpr} & ::= & \textit{AddExpr}\ \big(\ \big(\ \texttt{<}\ \mid\ \texttt{<=}\ \mid\ \texttt{>}\ \mid\ \texttt{>=}\ \big)\ \textit{AddExpr}\ \big)^{*} \\ \textit{AddExpr} & ::= & \textit{MulExpr}\ \big(\ \big(\ \texttt{+}\ \mid\ \texttt{-}\ \big)\ \textit{MulExpr}\ \big)^{*} \\ \textit{MulExpr} & ::= & \textit{UnaryExpr}\ \big(\ \big(\ \texttt{*}\ \mid\ \texttt{/}\ \mid\ \texttt{\%}\ \big)\ \textit{UnaryExpr}\ \big)^{*} \\ \textit{UnaryExpr} & ::= & \big(\ \texttt{!}\ \mid\ \texttt{-}\ \mid\ \texttt{*}\ \big)\ \textit{UnaryExpr}\ \mid\ \textit{PostfixExpr} \\ \textit{PostfixExpr} & ::= & \textit{Primary}\ \big(\ \texttt{[}\ \textit{Expr}\ \texttt{]}\ \mid\ \texttt{.}\ \textit{VarId} \\  & & \qquad \mid\ \texttt{->}\ \textit{VarId}\ \mid\ \textit{Args}\ \big)^{*} \\ \textit{Primary} & ::= & \textit{IntLit}\ \mid\ \texttt{true}\ \mid\ \texttt{false}\ \mid\ \texttt{nullptr}\ \mid\ \texttt{this}\ \mid\ \textit{VarId} \\  & \mid & \texttt{(}\ \textit{Expr}\ \texttt{)} \\  & \mid & \texttt{new}\ \big(\ \textit{ClassType}\ \mid\ \texttt{std::vector}\ \texttt{<}\ \textit{Type}\ \texttt{>}\ \big)\ \textit{Args} \\ \textit{Args} & ::= & \texttt{(}\ \big(\ \textit{ArgExpr}\ \big(\ \texttt{,}\ \textit{ArgExpr}\ \big)^{*}\ \big)^{?}\ \texttt{)} \\ \textit{ArgExpr} & ::= & \textit{Lambda}\ \mid\ \textit{Expr} \\ \textit{Lambda} & ::= & \texttt{[=]}\ \textit{Params}\ \texttt{->}\ \textit{Type}\ \textit{Block} \end{array}`

A statement is a command or an expression followed by `;`. Assignment is a command, and `ExprStatement` joins the two forms that start with an expression to keep the grammar LL(1). In the abstract syntax the commands form the type `Cmd`, and the expression statement is the command `exprStmt`, which evaluates and discards the value. The left side of an assignment must denote a location, a check made by the type checker. A lambda expression is an `ArgExpr` and not a `Primary`, so it occurs only as argument, as initialiser of a declaration and as `return` expression.
:::

:::theorem "gram_ll1" (parent := "ud1") (lean := "CoreCpp.Grammar.isLL1_grammar, CoreCpp.LL1.Grammar.isLL1, CoreCpp.LL1.Grammar.table, CoreCpp.LL1.Grammar.parse")
The grammar is LL(1).
:::

:::proof "gram_ll1"
The proof is a computation. The theorem `CoreCpp.Grammar.isLL1_grammar` builds the predictive parsing table of the grammar of {bpref "gram_full"}[] and checks that no entry holds two productions.

Two modules carry the computation. The module `CoreCpp/Grammar.lean` states the grammar rule by rule over token classes. The module `CoreCpp/LL1.lean` translates it from EBNF to BNF. It then computes the nullable nonterminals, FIRST and FOLLOW as least fixed points, and fills the table.

Two left factorings explain the result. In `Member`, a `TypeId` opens both a constructor and the type of a field or method. The next token decides, `(` for a constructor and any other token for a type. In `Statement`, a type opens `LocalDecl` and an expression opens `ExprStatement`. A type starts with `int`, `bool`, `void`, `std::function`, `std::vector` or a `TypeId`. An expression starts with a `VarId`, an `IntLit`, `true`, `false`, `nullptr`, `this`, `new`, `(`, `!`, `-` or `*`, and never with a `TypeId`. The two FIRST sets are therefore disjoint.

The same table drives the nonrecursive predictive parser `CoreCpp.LL1.Grammar.parse`.
:::

# Abstract syntax and parser

:::definition "gram_ast" (parent := "ud1") (lean := "CoreCpp.Ty, CoreCpp.UnOp, CoreCpp.BinOp, CoreCpp.Expr, CoreCpp.Cmd, CoreCpp.Param, CoreCpp.Fun, CoreCpp.ClassDecl, CoreCpp.Decl, CoreCpp.Program") 
The abstract syntax, the tree form of the grammar of {bpref "gram_full"}[], has one inductive type per class of nonterminals. `Ty` has the basic types, class, pointer, vector and function types and the internal type of `nullptr`. `Expr` has literals, `nullptr`, variable, unary and binary operators, conditional, call, the two forms of `new`, field access, arrow, dereference, indexing, the lambda and the call of a function value. A `Param` may be by reference. `Cmd` has block, `if`, `while`, `for`, `return`, declaration with a type or with `auto`, assignment and expression statement. A `Fun` has a return type, a name, parameters and a body. A `ClassDecl` has a qualified name, an optional base, fields and methods with their visibility, at most one constructor and at most one destructor. `Expr` also has `this`, the method call and the `new` with arguments, and `Cmd` has `delete`. A `Program` is a list of declarations. The abstract grammar of the implemented subset follows, with one alternative per constructor of `Syntax.lean` and in the same order, the constructor named in the right column.

$$`\begin{array}{lcll} \tau & ::= & \mathtt{int} \mid \mathtt{bool} \mid \mathtt{void} \mid C \mid \tau\mathtt{*} \mid \mathtt{std{:}{:}vector}\langle\tau\rangle \mid \mathtt{std{:}{:}function}\langle\tau(\tau_1, \ldots, \tau_k)\rangle \mid \mathsf{nullptr\_t} & \textsf{Ty} \\ \ominus & ::= & \mathtt{!} \mid \mathtt{-} & \textsf{UnOp} \\ \oplus & ::= & \mathtt{+} \mid \mathtt{-} \mid \mathtt{*} \mid \mathtt{/} \mid \mathtt{\%} \mid \mathtt{==} \mid \mathtt{!=} \mid \mathtt{<} \mid \mathtt{<=} \mid \mathtt{>} \mid \mathtt{>=} \mid \mathtt{\&\&} \mid \mathtt{||} & \textsf{BinOp} \\ e & ::= & n \mid b \mid \mathtt{nullptr} \mid x \mid \ominus\, e \mid e_1 \oplus e_2 \mid e_1\ \mathtt{?}\ e_2\ \mathtt{:}\ e_3 \mid f(e_1, \ldots, e_k) & \textsf{Expr} \\   & \mid & \mathtt{new}\ C(e_1, \ldots, e_k) \mid \mathtt{new}\ \mathtt{std{:}{:}vector}\langle\tau\rangle(e) \mid e.f \mid e\mathtt{->}f \mid \mathtt{*}e \mid e_1[e_2] & \textsf{Expr.newObj, newVec, field, arrow, deref, index} \\   & \mid & \mathtt{[=]}(p_1, \ldots, p_k)\ \mathtt{->}\ \tau\ \{\, c_1 \ldots c_n \,\} \mid e(e_1, \ldots, e_k) & \textsf{Expr.lambda, callFn} \\   & \mid & \mathtt{this} \mid e.m(e_1, \ldots, e_k) \mid e\mathtt{->}m(e_1, \ldots, e_k) & \textsf{Expr.this, methodCall} \\ c & ::= & \{\, c_1 \ldots c_n \,\} & \textsf{Cmd.block} \\   & \mid & \mathtt{if}\ (e)\ \{\, c_1 \ldots c_n \,\}\ \mathtt{else}\ \{\, c'_1 \ldots c'_m \,\} & \textsf{Cmd.ite} \\   & \mid & \mathtt{while}\ (e)\ \{\, c_1 \ldots c_n \,\} & \textsf{Cmd.while} \\   & \mid & \mathtt{for}\ (c_0;\ e;\ c_s)\ \{\, c_1 \ldots c_n \,\} & \textsf{Cmd.for} \\   & \mid & \mathtt{return} \mid \mathtt{return}\ e & \textsf{Cmd.ret} \\   & \mid & \tau\ x = e \mid \tau\mathtt{\&}\ x = e \mid \mathtt{auto}\ x = e & \textsf{Cmd.decl, Cmd.declRef, Cmd.declAuto} \\   & \mid & e_1 = e_2 & \textsf{Cmd.assign} \\   & \mid & e; & \textsf{Cmd.exprStmt} \\   & \mid & \mathtt{delete}\ e & \textsf{Cmd.delete} \\ p & ::= & \tau\ x \mid \tau\mathtt{\&}\ x & \textsf{Param} \\ F & ::= & \tau\ f(p_1, \ldots, p_k)\ \{\, c_1 \ldots c_n \,\} & \textsf{Fun} \\ K & ::= & \mathtt{class}\ C\ (\mathtt{:}\ \mathtt{public}\ B)^{?}\ \{\, M_1 \ldots M_n \,\} & \textsf{ClassDecl} \\ M & ::= & \tau\ f; \mid \tau\ m(p_1, \ldots, p_k)\ \{\, c_1 \ldots c_n \,\} \mid C(p_1, \ldots, p_k)\ \{\, c_1 \ldots c_n \,\} \mid \mathtt{\sim}C()\ \{\, c_1 \ldots c_n \,\} & \textsf{Field, Method, Ctor, Dtor} \\ D & ::= & K \mid F & \textsf{Decl} \\ P & ::= & D_1 \ldots D_n & \textsf{Program} \end{array}`

Here $`n` is an integer, $`b` a boolean, $`x` a variable identifier and $`f` a function identifier. The abstract syntax has no parentheses and no precedence, because the tree fixes the structure.
:::

:::definition "parse_expr" (parent := "ud1") (lean := "CoreCpp.expr, CoreCpp.orExpr, CoreCpp.andExpr, CoreCpp.eqExpr, CoreCpp.relExpr, CoreCpp.addExpr, CoreCpp.mulExpr, CoreCpp.unaryExpr, CoreCpp.postfixExpr, CoreCpp.primary, CoreCpp.args, CoreCpp.argExpr, CoreCpp.lambda") (uses := "gram_ast, lex_automaton")
One parser function per expression nonterminal, from `Expr` down to `Primary`. Each level of the grammar is one level of precedence, and the repetition `( op Operand )*` builds a left associative tree. In the implemented subset `PostfixExpr` covers the function call, indexing, field access and arrow, `UnaryExpr` adds the dereference, and `Primary` covers literals, `nullptr`, variables, parenthesised expressions and the two forms of `new`. Lambdas are parsed as argument expressions, `argExpr`, in the three positions the grammar gives them. Method calls and `this` are not parsed yet.
:::

:::definition "parse_statement" (parent := "ud1") (lean := "CoreCpp.block, CoreCpp.statement, CoreCpp.localDecl, CoreCpp.forInit, CoreCpp.exprStatement") (uses := "gram_ast, parse_expr")
One parser function per command nonterminal. The function `statement` chooses the production by the first token, and `exprStatement` reads an expression and then decides between assignment and expression statement by the presence of `=`. In the implemented subset `Type` is a basic type, a class name optionally followed by `*`, `std::vector<Type>` optionally followed by `*`, or `std::function<Type(Type, …)>`. A local declaration may be a reference, and its initialiser may be a lambda.
:::

:::definition "parse_program" (parent := "ud1") (lean := "CoreCpp.program, CoreCpp.declaration, CoreCpp.classDecl, CoreCpp.member, CoreCpp.classType, CoreCpp.function, CoreCpp.params, CoreCpp.param, CoreCpp.type, CoreCpp.basicType, CoreCpp.runParser, CoreCpp.parseProgram, CoreCpp.parseExpr, CoreCpp.parseStatement") (uses := "gram_ast, parse_statement")
A program is a sequence of declarations up to `eof`, each a class with public fields or a function. The function `runParser` runs the lexer and a parser on a string and requires the whole input to be consumed. Namespaces, templates, methods and constructors are not parsed yet.
:::
